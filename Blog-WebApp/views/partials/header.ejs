<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Shaheer's Blog Web App</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Playfair+Display:700,900&amp;display=swap">
  <link rel="icon" type="image/x-icon" href="/img/favicon.ico">  
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <link rel="stylesheet" href="css/bootstrap-social.css">
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
  <div class="nav-scroller py-1 mb-3 border-bottom">
    <nav class="nav nav-underline justify-content-center">
      <div class="col-4">
        <a class="blog-header-logo text-body-emphasis text-decoration-none" href="/">blogToday!</a>
      </div>
      
      <a class="nav-item nav-link link-body-emphasis <% if (address === "/") { %>active<% } %>" href="/">Home</a>
      <a class="nav-item nav-link link-body-emphasis <% if (address === ("/users/" + userID)) { %>active<% } %>" href="<%= auth ? "/users/" + userID : "/login" %>">User Profile</a>
      <a class="nav-item nav-link link-body-emphasis <% if (address === "/compose") { %>active<% } %>" href="<%= auth ? "/compose" : "/login" %>">Compose</a>
      
      <div class="col-4 d-flex justify-content-end align-items-center">
        <a class="logout nav-link" href="<%= auth ? "/logout" : "/login" %>">
          <%= auth ? "Log Out" : "Log In" %>
        </a>
      </div>
    </nav>
  </div>